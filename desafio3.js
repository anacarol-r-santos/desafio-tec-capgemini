function anagramas(string) {
   let ordenaString = string.split('').sort().join('');
   let vogais = [a, e, i, o, u]

   for (let indexVogal = 0; indexVogal <= vogais.length; indexVogal++) {
       for (let indexString = 0; indexString <= ordenaString.length; indexString++) {
           const element = array[index];
           
       }   

       
   }

   return console.log(ordenaString);
    
}

// anagramas('banana');

module.exports = { anagramas };